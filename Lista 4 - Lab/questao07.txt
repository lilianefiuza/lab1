#include<stdio.h>
#include<conio.h>
#include<string.h>

int main()
{	
	char frase[30],vogal,maiuscula;
	int i;
	printf("digite uma frase: ");
	scanf("%c",frase);
	maiuscula = strupr(frase);
	vogal={'A','E','I','O','U'};
	for (i=0; i==strlen(frase);i++)
	{
		if( frase[i] == vogal)
		{
			frase[i] = frase[i+1];
		}
	}
	printf("%s",frase);
}