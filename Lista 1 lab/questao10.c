#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(void)
{
	float raio;
	float area;
	float pi;
	printf("Para saber a area do circulo informe o valor do raio ");
	scanf("%f", &raio);
	pi = (3,141592);
	area = (pi * (raio^2))
	scanf("%f", &area);
	printf ("o valor da a area eh %f", &area);
	return 0;
}
