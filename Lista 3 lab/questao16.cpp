#include <stdio.h>
#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() 
{
	int A.B,N,soma,i;
	A = 0;
	B = 1;
	for (i=3; N <4.000.000 ; i++ )
	{
		N=A+B;
		A=B;
     	B=N;
     	if (N%2 ==0)
     	{
     		soma = soma +N
     	}
	}
	printf ("%d",soma);
	return 0;
}
